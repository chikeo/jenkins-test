const expect = require('chai').expect;

// Trigger Jenkins Job for Github PR
// This is a test
describe('TEST', () => {
  it('passed', () => {
    expect(true).to.be.true;
  });
});

console.log('here');
