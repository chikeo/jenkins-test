const expect = require('chai').expect;

// Trigger Jenkins buid on Pull Request
// This is a test
describe('TEST', () => {
  it('passed', () => {
    expect(true).to.be.true;
  });
});

console.log('here');
